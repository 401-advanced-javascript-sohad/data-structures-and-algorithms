'use strict';

const array = [1,2,3,4,5,6,7];
const element = 8;

const arrShift = (array, element) => {

  for(i = 0; i < array.length - 1; i++) {

    if(array[index] > array.length / 2) {

      let middleOfArray = array[index];

      console.log('Middle Array:', middleOfArray);
      let arrs = [...array, ...middleElement]
      console.log('start: ', arrs);
      break;
    };
  };
};
//not finish
// console.log('Call: ', arrShift(array,element));

module.exports = (insertShuftArray);